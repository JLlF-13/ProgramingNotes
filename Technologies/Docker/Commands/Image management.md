
List dowloaded images
```
docker images
```

Dowload an image from a registry
```
docker pull image[:tag]
```

Remove an image
```
docker rmi image[:tag]
```

Build an imag from a Dockerfile
```
docker build -t image_name path_to_dockerfile
```