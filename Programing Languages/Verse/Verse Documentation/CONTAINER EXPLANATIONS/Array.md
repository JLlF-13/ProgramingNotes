Una array es un contenidor on pots guardar elements del mateix tipus i accedir a ells per la seva posicio donant el index en la array.
Es te que tenir en compte que el index de la array sempre comença per 0 i podem accedir al ultim amb un -1

```verse
Players : []player = array{Player1, Player2}
```