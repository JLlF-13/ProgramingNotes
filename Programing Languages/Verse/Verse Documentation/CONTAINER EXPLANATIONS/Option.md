El contenidor de tipus option es un contenidor que pot ser null
En el exemple de abaix observem que la variable es MaybeANumber es un ?int sencer opcioanr que no conte cap valor, Despres es defineix un no u valor per a la variable MaybeANumber 

```verse
var MaybeANumber : ?int = false
set MaybeANumber := option{13}
```