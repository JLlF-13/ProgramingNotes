A **dictionary** stores data as key-value pairs. Keys must be unique and immutable, while values can be of any type. Dictionaries are great for fast lookups.

```python
person = {"name": "Alice", "age": 30}
print(person["name"])  
print(person["age"])  
```

Output: 
Alice
30