A **tuple** is an ordered, immutable collection. Once created, its contents cannot be changed. Tuples are useful for fixed data structures.

```python
coordinates = (10, 20)
print(coordinates[1]) 
```

Output: 20