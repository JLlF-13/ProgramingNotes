A **list** is an ordered, mutable collection that can hold items of any type. Lists allow duplicates and support indexing, slicing, and many built-in methods.

```python
fruits = ["apple", "banana", "cherry"]
fruits.append("orange")
print(fruits[0])  
```

Output: Apple