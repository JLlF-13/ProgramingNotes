A **set** is an unordered collection of unique elements. Sets are useful for membership tests, removing duplicates, and mathematical operations like union and intersection.

```python
colors = {"red", "green", "blue"}
colors.add("yellow")
print("red" in colors) 
print("purple" in colors) 
```

Output: 
True
False