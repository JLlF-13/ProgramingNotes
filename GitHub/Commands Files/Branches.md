#### Work with multiple branches to develop features in parallel.****

List/Show the branches
```
git branch
```

Create a new branch
```
git branch <branch-name>
```

Change to another branch
```
git switch <branch-name>
```

Delete a branch
```
git branch -d <branch-name>
```