#### Mark specific versions of your project for releases or reference.

Create a tag
```
git tag <tag-name>
```

Commit tag
```
git tag -a <tag-name> -m "message"
```

Delete tag
```
git tag -d <tag-name>
```

View tags
```
git tag
```

Push tag
```
git push origin --tags
```