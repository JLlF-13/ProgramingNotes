#### Undo mistakes, restore files, and clean your workspace.

Recovers the changes without merge
```
git fetch
```

Discard all local changes
```
git reset --hard HEAD
```

Reverts the changes of a commit
```
git revert <hash-commit>
```